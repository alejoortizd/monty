#include "monty.h"
/**
 * _nop - Entry Point
 * @stack: parameter to check
 * @count: parameter to check
 * Return: print list
 */
void _nop(stack_t **stack, unsigned int count)
{
	(void) stack;
	(void) count;
}
